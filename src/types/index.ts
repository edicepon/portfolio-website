export interface Project {
  title: string;
  description: string;
  technologies: string[];
  image: string;
}

export interface Blog {
  title: string;
  excerpt: string;
  date: string;
  slug: string;
}
